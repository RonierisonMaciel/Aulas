# Passos para executar crawler

1. Criar seu ambiente virtual

```python
python -m venv .venv
source .venv/bin/activate
```

2. Instalando as depedências

```bash
pip install -r requirements.txt
```

3. Executar o código

```python
python crawler.py
```